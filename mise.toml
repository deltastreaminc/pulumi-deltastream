# Root-level mise override file.
# Add provider-specific tool overrides here. This file is NOT autogenerated.
# See .config/mise.toml for the full tool declaration.
# Example: override golangci-lint to a different version for local experiments.

[tools]
golangci-lint = "2.12.2"  # must be built with go >= module's go version (1.25)
